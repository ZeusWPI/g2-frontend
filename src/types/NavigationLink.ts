/**
 * Link entry displayed in the navigation bar.
 */
export class NavigationLink {
    /**
     * Text inside the navigation bar.
     */
    text: string;

    /**
     * Path to the route.
     */
    to: string;

    /**
     * Icon displayed inside the sidebar (mobile-only)
     */
    icon: string;
}
