export class Project {
    // TODO: remove this once new API spec is available. Keeping it for now to have a working prototype.
    project_id: number;

    id: number;
    name: string;
    description: string;
    image: string;
}
